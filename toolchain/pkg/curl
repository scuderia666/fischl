ver 7.82.0

[src]
https://curl.haxx.se/download/%name-%ver.tar.xz

[build]
./configure \
	--prefix=%toolchain \
	--host=%target \
	--disable-static \
	--enable-threaded-resolver \
	--with-openssl \
	--with-ca-bundle=%toolchain/etc/ssl/cert.pem

%make && make install
